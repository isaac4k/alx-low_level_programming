#include <stdio.h>
/**
* main - Entry point
* @argc: the number of argument
* @argv: a pointer to argument strings
* Return: always 1
*/

int main(int argc, char *argv[])
{

	if (argc >= 0)
		puts(argv[0]);
	return (0);
}
