#include "main.h"
/**
 * add - add two integers
 * @n: input integer number
 * @m: input integer number
 * Return: always return the sum of two integers
 */
int add(int n, int m)
{
	int result;

	result = n + m;
	return (result);
}
